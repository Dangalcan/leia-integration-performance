services:
  locust:
    build: .
    ports:
      - "8089:8089"
    volumes:
      - ./sessions:/app/sessions:ro
      - ./reports:/app/reports
    env_file:
      - .env
    environment:
      - SESSIONS_DIR=/app/sessions
